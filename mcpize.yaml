# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "Medical Trend"
slug: "medical-trend-3"

runtime: python
entry: "backend/main.py"

build:
  install: "pip install -r backend/requirements.txt"

start:
  type: stdio
  command: "python"
  args:
    - "backend/main.py"

bridge:
  mode: stdio

credentials:
  - name: "ANTHROPIC_API_KEY"
